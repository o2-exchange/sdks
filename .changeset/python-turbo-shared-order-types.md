---
sdk-python: minor
---
Added `OrderType.TURBO_SHARED_SPOT` and `OrderType.TURBO_SHARED_POST_ONLY`. These are the House-only order types that the order book shares with its Turbo sidecar. They encode as `OrderType` variants 6 and 7.
